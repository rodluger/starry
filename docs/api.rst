The starry API
==============

Below you'll find links to the :py:mod:`starry` API and all functionality
accessible to the user. There are two flavors of the API: plain vanilla
:py:mod:`starry`, which computes light curves, phase curves, and all the
other good stuff; and the *autograd* version of :py:mod:`starry`, which
does all that **and** computes gradients with respect to the input
parameters using (analytical) autodifferentiation.

.. toctree::
   :maxdepth: 1

   starry <starry>
   starry.grad <starry_grad>
